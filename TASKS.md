### Features

- [x] we need to create the functionality now to allow the user to trigger a search and present the results. there should be an input for the keyword to search for, return the results, with pictures, if available, and then allow the user to request. when something is requested, there should be a check if it is approved. if not, perform a request to approve it
- [x] we need to create the functionality to allow the user to approve pending requests
